clear all; close all; clc;

% Load data.
load('data.mat');

% Compute pitch angles with EKF.
[pitch_EKF_right_thigh, pitch_EKF_right_shank, p] = ...
            fusion_EKF(g_Y_right_thigh_1_C', ...
                       g_Y_right_shank_1_C', ...
                       a_X_right_thigh_1_C', ...
                       a_Z_right_thigh_1_C', ...
                       a_X_right_shank_1_C', ...
                       a_Z_right_shank_1_C', ...
                       f, 0.25, 0.25);

% Plot.
n = 4000;
figure1 = figure(1);
hold on
% plot(time(1:n), -pitch_acc_right_shank(1:n), ...
%      time(1:n), -pitch_gyro_right_shank(1:n), ...
%      time(1:n), -pitch_KF_right_shank(1:n), ...
%      time(1:n), -pitch_GKF_right_shank(1:n), ...
  plot(time(1:n), (pitch_EKF_right_shank(1:n) ...
                 + pitch_EKF_right_thigh(1:n)), ...
     time(1:n), p(1:n));
xlabel('Time $t$ in s', 'interpreter','latex');
ylabel(['Pitch angle $\theta_1 + \theta_2$ in ', ...
        '$^{\circ}$'], 'interpreter','latex');
% legend('Accelerometer-based', ...
%        'Integration of angular rate', ...
%        'Kalman filter', 'Gated Kalman filter', ...
legend('Extended Kalman filter', 'Accelerometer-based');
   
matlab2tikz('../images/experiment_1.tikz', 'height', ...
            '\figureheight', 'width', '\figurewidth');
        
% Plot.
n = length(a_X_right_shank_1_C);
figure2 = figure(2);
hold on
plot(time(1:n), -pitch_acc_right_shank(1:n), ...
     time(1:n), -pitch_gyro_right_shank(1:n), ...
     time(1:n), -pitch_KF_right_shank(1:n), ...
     time(1:n), -pitch_GKF_right_shank(1:n), ...
     time(1:n), (pitch_EKF_right_shank(1:n) ...
                 + pitch_EKF_right_thigh(1:n)), ...
     time(1:n), p(1:n));
xlabel('Time $t$ in s', 'interpreter','latex');
ylabel(['Pitch angle $\theta_1 + \theta_2$ in ', ...
        '$^{\circ}$'], 'interpreter','latex');
legend('Accelerometer-based', ...
       'Integration of angular rate', ...
       'Kalman filter', 'Gated Kalman filter', ...
       'Extended Kalman filter', 'p');
        